# Work Queue Prompts Part 02

## W026 (L169, git_deploy, unknown)

현재 /login 페이지가 아직 wadeal-test 기준 카카오 설정 안내와 기존 커스텀 OAuth 설정을 사용하고 있어. 새 Kakao 앱 기준으로 정리해줘.

1. /login 화면의 wadeal-test 안내 문구 제거
2. 기존 커스텀 Kakao OAuth 흐름 제거
3. Supabase 기본 OAuth signInWithOAuth({ provider: "kakao" }) 방식만 사용
4. scope는 profile_nickname profile_image만 요청
5. 수정 후 Vercel 운영 배포까지 진행

---

## W027 (L176, general, unknown)

새로 만든 카톡 앱이야. 그전꺼는 삭제햇어. 이거로 업데이트해주면되

---

## W028 (L177, general, unknown)

이거도 참고해줘

---

## W029 (L184, git_deploy, unknown)

카카오 로그인은 성공하지만 /mypage 에서 세션을 읽지 못하고 "로그인이 필요해요"가 뜹니다.

Supabase auth session 유지 문제를 수정해주세요.

확인할 것:
1. createClient()가 동일한 Supabase project URL/key 사용하는지
2. middleware.ts 에서 auth session 전달하는지
3. mypage 에서 supabase.auth.getSession() 또는 getUser() 정상 호출되는지
4. 로그인 후 redirect 과정에서 session cookie/localStorage 유지되는지
5. client component와 server component auth mismatch 수정
6. production Vercel 환경 기준으로 수정 후 redeploy

---

## W030 (L193, auth, unknown)

카카오 로그인/마이페이지 세션 문제는 잠시 보류하고, 다음 기능으로 넘어가자.

가격 알림 저장 기능을 구현해줘.

목표:
1. /alert/[id] 에서 사용자가 원하는 목표 가격을 입력/선택
2. 저장 버튼을 누르면 Supabase에 저장
3. 로그인 세션이 있으면 user_id와 연결
4. 세션이 없으면 일단 비로그인 상태 안내만 표시
5. /mypage/alerts 페이지를 만들고 저장된 가격 알림 목록 표시
6. 실제 카카오톡 메시지 발송은 아직 구현하지 않음
7. 기존 상품/공동구매/체크아웃 화면은 건드리지 말 것

---

## W031 (L218, auth, unknown)

이전 카카오 로그인 정리 작업은 마무리하고, 이제 가격 알림 기능 구현 작업만 진행해줘.

---

## W032 (L224, auth, unknown)

가격 알림 저장 기능을 구현해줘.

목표:
1. /alert/[id] 에서 사용자가 원하는 목표 가격을 입력/선택
2. 저장 버튼을 누르면 Supabase에 저장
3. 로그인 세션이 있으면 user_id와 연결
4. 세션이 없으면 일단 비로그인 상태 안내만 표시
5. /mypage/alerts 페이지를 만들고 저장된 가격 알림 목록 표시
6. 실제 카카오톡 메시지 발송은 아직 구현하지 않음
7. 기존 상품/공동구매/체크아웃 화면은 건드리지 말 것

---

## W033 (L229, auth, unknown)

카카오톡으로 알림 받기 눌러도 다음으로 넘어가지 않아.

---

## W034 (L234, auth, unknown)

/alert/[id] 화면에서 ‘카카오톡으로 알림받기’ 버튼이 눌리지 않습니다. 버튼 클릭 시 목표 가격 저장 로직이 실행되게 연결해주세요.

요구사항:
1. 목표 가격이 선택/입력되어 있어야 저장 가능
2. 로그인 세션이 없으면 로그인 필요 안내 표시
3. 로그인 세션이 있으면 price_alerts 테이블에 insert
4. 저장 성공하면 /mypage/alerts 로 이동
5. 실패하면 화면에 에러 메시지 표시
6. 기존 상품/체크아웃/공동구매 화면은 건드리지 말 것

---

## W035 (L240, auth, unknown)

/alert/[id]의 ‘카카오톡으로 알림받기’ 버튼이 여전히 눌리지 않습니다.

확인해서 수정해주세요:
1. button disabled 조건을 점검하고, 왜 비활성화되는지 화면에 표시
2. targetPrice가 없으면 “목표 가격을 선택해주세요” 표시
3. session이 없으면 버튼 클릭 가능하게 두고, 클릭 시 /login?next=/alert/[id] 로 이동
4. session이 있으면 price_alerts insert 실행
5. 성공 시 /mypage/alerts 로 이동
6. onClick/onSubmit 이벤트가 실제 연결되어 있는지 확인
7. form 내부 button type을 submit 또는 button으로 올바르게 수정

---

## W036 (L247, auth, unknown)

/alert/[id] 가격 알림 설정 버튼이 계속 안 눌립니다. 복잡한 조건 제거하고 버튼 클릭 테스트부터 되게 단순화해주세요.

수정:
1. ‘카카오톡으로 알림받기’ 버튼의 disabled 조건 전부 제거
2. 버튼 onClick에 alert("버튼 클릭됨") 먼저 넣어서 클릭 이벤트가 먹는지 확인
3. 클릭이 먹으면 그 다음 price_alerts insert 로직 연결
4. 버튼 위에 투명 div나 overlay가 클릭을 막고 있는지 z-index/pointer-events 확인
5. form submit 문제면 form 제거하고 일반 button type="button"으로 변경
6. 저장 성공 시 /mypage/alerts 이동
7. 기존 다른 화면은 건드리지 말 것

---

## W037 (L252, auth, unknown)

현재 가격 알림 기능 방향이 잘못됐습니다.

카카오톡 실제 발송은 아직 구현하지 않습니다.
버튼 이름도 혼란스러우니 일단 “가격 알림 저장하기”로 바꿔주세요.

필수 수정:
1. /alert/[id] 화면에 목표 가격 선택 항목을 3개 이상 보여주세요.
   예: 현재가보다 5% 하락, 10% 하락, 직접 입력
2. “가격 알림 저장하기” 버튼은 무조건 클릭 가능해야 합니다.
3. 클릭 시 로그인 상태면 price_alerts 테이블에 저장
4. 비로그인 상태면 /login?next=/alert/[id] 로 이동
5. 저장 성공하면 /mypage/alerts 로 이동
6. 실제 카카오톡 메시지 발송 코드는 넣지 마세요.
7. “카카오톡으로 알림받기” 문구는 지금 단계에서는 제거해주세요./alert/[id] 화면의 “카카오톡으로 알림받기/가격 알림 설정하기” 네모 영역 전체가 클릭 가능해야 합니다.

현재는 중간 글씨에 마우스를 올렸을 때만 pointer cursor가 바뀌고 클릭됩니다.

수정해주세요:
1. 네모 카드 전체를 button 태그로 만들거나, 카드 전체에 onClick 연결
2. 전체 영역에 cursor-pointer 적용
3. 전체 영역에 hover 효과 적용
4. 내부 텍스트만 클릭되는 구조 제거
5. button 안에 다른 button/a 태그가 중첩되어 있으면 제거
6. 클릭 시 즉시 “알림 설정되었습니다” 메시지 표시
7. 선택한 목표 가격도 함께 저장되게 유지

---

## W038 (L255, auth, unknown)

현재 가격 알림 기능 방향이 잘못됐습니다.

카카오톡 실제 발송은 아직 구현하지 않습니다.
버튼 이름도 혼란스러우니 일단 “가격 알림 저장하기”로 바꿔주세요.

필수 수정:
1. /alert/[id] 화면에 목표 가격 선택 항목을 3개 이상 보여주세요.
   예: 현재가보다 5% 하락, 10% 하락, 직접 입력
2. “가격 알림 저장하기” 버튼은 무조건 클릭 가능해야 합니다.
3. 클릭 시 로그인 상태면 price_alerts 테이블에 저장
4. 비로그인 상태면 /login?next=/alert/[id] 로 이동
5. 저장 성공하면 /mypage/alerts 로 이동
6. 실제 카카오톡 메시지 발송 코드는 넣지 마세요.
7. “카카오톡으로 알림받기” 문구는 지금 단계에서는 제거해주세요./alert/[id] 화면의 “카카오톡으로 알림받기/가격 알림 설정하기” 네모 영역 전체가 클릭 가능해야 합니다.

현재는 중간 글씨에 마우스를 올렸을 때만 pointer cursor가 바뀌고 클릭됩니다.

수정해주세요:
1. 네모 카드 전체를 button 태그로 만들거나, 카드 전체에 onClick 연결
2. 전체 영역에 cursor-pointer 적용
3. 전체 영역에 hover 효과 적용
4. 내부 텍스트만 클릭되는 구조 제거
5. button 안에 다른 button/a 태그가 중첩되어 있으면 제거
6. 클릭 시 즉시 “알림 설정되었습니다” 메시지 표시
7. 선택한 목표 가격도 함께 저장되게 유지

---

## W039 (L261, general, unknown)

현재 작업 상태 저장해주세요. 다음 작업에서 /alert/[id] 버튼 클릭 영역 및 price alert 저장 UX부터 다시 점검할 예정입니다.

---

## W040 (L266, auth, unknown)

/alert/[id] 가격 알림 버튼이 계속 안 눌리는 문제부터 디버깅해주세요.

우선 저장/Supabase/카카오톡 로직은 잠시 빼고, 클릭 테스트만 되게 해주세요.

요구사항:
1. 버튼 전체 네모 영역이 클릭 가능해야 함
2. cursor-pointer가 네모 전체에 적용되어야 함
3. 클릭 시 즉시 “알림 설정되었습니다” 메시지 표시
4. console.log("alert button clicked") 출력
5. 버튼 위를 막는 overlay, absolute div, pointer-events 문제 확인
6. button 안에 button/a 태그 중첩되어 있으면 제거
7. 먼저 클릭이 확실히 되는 상태로 만든 뒤 저장 로직 연결

---

## W041 (L283, general, unknown)

커서(pointer)는 전체 영역에 정상 적용됐습니다. 이제 버튼 클릭 이벤트만 안 먹습니다.

확인해주세요:
1. onClick 함수가 실제 연결되어 있는지
2. button type="button"인지
3. form submit이 이벤트를 막고 있는지
4. e.preventDefault() 때문에 중단되는지
5. 클릭 시 alert("clicked")부터 먼저 실행되게 테스트

---

## W042 (L287, auth, unknown)

/alert/[id] 가격 알림 버튼 UX를 먼저 완성해주세요.

1. 버튼 문구: “가격 알림 설정하기”
2. 버튼 클릭 시 즉시 브라우저 confirm/alert 창 표시:
   “알림 설정되었습니다. 가격이 목표가에 도달하면 카카오톡 알림을 보내드릴게요.”
3. 확인 버튼 누르면 화면에도 “알림 설정 완료” 메시지 표시
4. 버튼 텍스트를 “설정 완료”로 변경
5. 클릭 이벤트가 반드시 실행되도록 onClick 직접 연결
6. 목표 가격 선택 항목 추가:
   - 5% 하락
   - 10% 하락
   - 직접 입력
7. 로그인 상태면 price_alerts 테이블에 저장
8. 비로그인 상태면 /login?next=/alert/[id] 로 이동
9. 실제 카카오톡 발송은 아직 구현하지 말고, price_alerts에 kakao_notify_status = 'pending' 같은 상태값만 남겨주세요.
10. 다음 단계에서 카카오 알림톡/비즈메시지 API 연동할 수 있게 코드 주석으로 TODO 남겨주세요.

---

## W043 (L293, auth, unknown)

Wadeal 프로젝트에서 /alert/[id] 가격 알림 설정 화면을 수정해줘.

목표:
1. "카카오톡으로 알림받기" 버튼 또는 알림 설정 버튼을 클릭하면 실제 카카오 메시지 발송은 하지 않는다.
2. 대신 브라우저 alert 창으로 "가격 알림이 설정됐어요." 메시지를 띄운다.
3. 확인 버튼을 누르면 alert 창이 닫히면 된다.
4. 버튼 전체 영역이 클릭 가능해야 한다.
5. cursor-pointer가 버튼 전체에 적용되어야 한다.
6. 나중에 카카오톡 알림 API 연동할 수 있도록 TODO 주석을 남겨줘.
7. Supabase 저장이나 카카오 메시지 발송은 아직 구현하지 마.

---

## W044 (L302, auth, unknown)

Wadeal 프로젝트에서 가격 알림 설정하기 버튼이 눌리지 않는 문제를 고쳐줘.

대상:
- /alert/[id] 페이지
- "가격 알림 설정하기"
- "카카오톡으로 알림받기" 버튼 또는 클릭 영역

요구사항:
1. 버튼 태그에 onClick 이벤트를 직접 연결해줘.
2. 클릭 시 alert("가격 알림이 설정됐어요.")가 뜨게 해줘.
3. 버튼 전체 영역이 클릭 가능해야 해.
4. className에 cursor-pointer를 넣어줘.
5. 버튼 위에 투명 div, Link, disabled, pointer-events-none 같은 클릭 방해 요소가 있으면 제거해줘.
6. form 안에 있다면 type="button"을 넣어줘.
7. 실제 Supabase 저장, 카카오톡 발송은 아직 하지 말고 TODO 주석만 남겨줘.
8. 수정 후 /alert/[id]에서 버튼 클릭이 되는지 확인할 수 있게 해줘.

---

## W045 (L319, auth, unknown)

/alert/[id] 가격 알림 화면을 수정해줘.

요구사항:
1. 5% 하락, 10% 하락, 목표 가격 직접 입력 카드가 클릭되게 해줘.
2. 선택한 카드에는 빨간 테두리/배경/체크 상태가 보이게 해줘.
3. 하단 버튼 문구를 "카카오톡으로 알림받기"로 바꿔줘.
4. 하단 버튼 디자인은 카카오 노란색 버튼으로 바꿔줘.
5. 하단 버튼은 실제 button 태그로 만들고 type="button"을 넣어줘.
6. 버튼 전체에 onClick을 연결해줘.
7. 클릭하면 화면 중앙에 모달창을 띄워줘.

모달창 내용:
- 제목: 알림 설정 완료
- 문구: 목표 가격에 도달하면 카카오톡으로 알려드릴게요.
- 버튼: 확인

8. 확인 버튼을 누르면 모달창이 닫히게 해줘.
9. 실제 카카오톡 메시지 발송은 아직 하지 말고 TODO 주석만 남겨줘.
10. disabled, pointer-events-none, 투명 overlay 등 클릭을 막는 요소가 있으면 제거해줘.

---

## W046 (L324, auth, unknown)

현재가보다 5프로 하락 10프로하락 목표가격 직접 설정 버튼 옵션 선택하는게 안되,  알림설정하기도 버튼도 안되. 일단 바꿀거는 가격 알림 설정하기는 카카오톡색 노란색으로 바꾸고. 알림 메세지 받기로 글 바꿔줘. 이 버튼을 눌럿을때 알림설정이 됫다는 메세지 창이 중간에 띄워져야해. 그리고 확인 버튼 누르면 이 메세지 창이 꺼지고. 나중에 카카오톡 메세지랑 연동할거야

---

## W047 (L338, general, unknown)

오 됫어. 알림 메세지 받기 버튼 박스 빨간색 박스로 만들어줘

---

## W048 (L342, auth, unknown)

나중에 카카오톡 메세지 알림 받기랑. 알림 메세지 받기랑 연동 시킬거야.

---

## W049 (L347, mypage, unknown)

다음 단계는 가격 알림을 “내 알림 목록”에 보이게 만들기야.

지금 목표:

알림 설정 완료 → 확인 누르면 → /mypage/alerts 이동 → 내가 설정한 알림 카드 보임 / Wadeal에서 가격 알림 설정 완료 후 내 알림 목록으로 이동하는 흐름을 만들어줘.

요구사항:
1. /mypage/alerts 페이지를 새로 만들어줘.
2. 페이지 제목은 "내 가격 알림"으로 해줘.
3. 알림 카드 예시는 1개 보여줘:
   - 상품명: 제주 고당도 감귤 3kg
   - 현재가: 12,900원
   - 목표가: 12,255원
   - 상태: 알림 대기중
4. /alert/[id]에서 알림 설정 완료 모달의 "확인" 버튼을 누르면 /mypage/alerts 로 이동하게 해줘.
5. 아직 Supabase 저장은 하지 말고 화면 흐름만 만들어줘.
6. 나중에 Supabase alerts 테이블에 저장할 수 있도록 TODO 주석만 남겨줘.

---

## W050 (L354, general, unknown)

아까까지 가격 알림 설정 버튼 누르면 다 됬는데. 이제는 페이지가 안넘어가네

---
