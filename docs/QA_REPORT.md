# CELLOH mobile-ui — 런타임 QA 보고서

**검수일:** 2026-05-30  
**브랜치:** `mobile-ui` · **HEAD:** `7e9fce2` (+ 미커밋 버그픽스 2건)  
**환경:** localhost:3000 (Next.js 16.2.6 dev)  
**범위:** 실사용자 플로우 — 홈 → 검색 → 상품상세 → 장바구니 → 로그인 → 마이페이지  
**제약:** 기능 삭제·디자인 변경·DB 변경 없음

---

## 1. 시나리오별 결과

### 1️⃣ 홈 (`/`)

| 항목 | 결과 |
|------|------|
| HTTP | **200** |
| 상품 카드 링크 | `/product/{id}` 16건 + slug 1건 — **전부 200** |
| 검색 진입 | 헤더 검색폼 → `encodeURIComponent` 후 `/search?q=` |
| 카테고리/추천 검색 | `/search?q=신규` 등 링크 정상 |
| 장바구니 아이콘 | `/join-cart` → 미로그인 **307** → login `next=/join-cart` |
| 마이 아이콘 | `/mypage` → **307** → login `next=/mypage` |
| 하단 nav | 홈·찜(`/saved` 200)·마이(307) 정상 |

**판정:** PASS

---

### 2️⃣ 검색 (`/search`)

| 경로 | HTTP | UI |
|------|------|-----|
| `/search` | 200 | idle hub (추천·인기·판매자) |
| `/search?q=` | 200 | idle hub (`trim` → empty) |
| `/search?q=test` | 200 | 결과 또는 empty + fallback 추천 |
| `/search?q=__no_result_xyz__` | 200 | 「검색 결과가 없습니다」+ 추천 영역 |
| `/search?cat=food` | 200 | 카테고리 필터 |
| 한글 `q` (URL-encoded) | 200 | 정상 (curl raw UTF-8는 400 — 브라우저 자동 인코딩 OK) |

**판정:** PASS

---

### 3️⃣ 상품 상세 (`/product/1`)

| 항목 | 결과 |
|------|------|
| HTTP | **200** |
| sticky CTA | 구매하기 → `/join/wd-citrus-001` (200) |
| 장바구니 버튼 | server action → 미로그인 `login_required` |
| 찜 버튼 | `stopPropagation` (카드 링크 충돌 없음) |
| 판매자 패널 | `/sellers/` 경로 사용 |
| not-found | `/product/99999` → not-found UI (dev HTTP 200) |

**판정:** PASS

---

### 4️⃣ 장바구니 (`/join-cart`)

| 항목 | 결과 |
|------|------|
| 미로그인 직접 접근 | **307** → `/login?next=/join-cart` |
| 로그인 후 (prototype) | **200** · empty state 「구매 검토함이 비어 있어요」 |
| empty → 홈 CTA | `actionHref="/"` |
| cart item 링크 | `/product/{slug}` |

**발견 버그 (P1):**  
미로그인 상태에서 상품 상세 「장바구니」 클릭 → 로그인 → **상품이 자동으로 담기지 않음**  
(기존: `next=/product/{slug}` → 로그인 후 상품 페이지만 복귀)

**수정 (미커밋):**

| 파일 | 변경 |
|------|------|
| `components/add-to-join-cart-button.tsx` | login `next=/join-cart?pending={slug}` |
| `app/join-cart/page.tsx` | 로그인 후 `pending` slug 자동 `addToJoinCartForUser` → redirect |

**판정:** PASS (수정 후) · Supabase `join_cart` 테이블 없으면 empty 유지 (환경 제한)

---

### 5️⃣ 로그인 (`/login`)

| 항목 | 결과 |
|------|------|
| HTTP | **200** |
| `next` / `redirect` | `safeRedirectPath` — middleware `next` 파라미터 보존 |
| `/login?next=/join-cart` | redirect 값 페이지에 반영 |
| OAuth | Google/Kakao 버튼 존재 · **실로그인 미검** |
| dev prototype | `wadeal_prototype_session=1` 쿠키 → protected route **200** |

**판정:** PASS (코드) · OAuth 실검 **미완**

---

### 6️⃣ 마이페이지 (`/mypage`)

| 항목 | 결과 |
|------|------|
| 미로그인 | **307** → login `next=/mypage` |
| prototype 로그인 | **200** · 대시·메뉴 렌더 |
| 하위 경로 | `/mypage/orders` 등 → 미로그인 307, 로그인 200 |
| `/saved` (찜) | 미로그인 **200** + inline 로그인 CTA (middleware exempt) |

**판정:** PASS

---

## 2. 인증 후 전체 플로우 (prototype session)

```
홈(200) → 상품(200) → [장바구니 클릭] → login?next=/join-cart?pending=…
→ (로그인) → join-cart pending 처리 → join-cart(200)
→ mypage(200) → orders(200)
```

| 경로 | prototype cookie |
|------|------------------|
| `/join-cart` | 200 |
| `/mypage` | 200 |
| `/checkout/wd-citrus-001` | 200 |
| `/notifications` | 200 |

---

## 3. 모바일 viewport (코드 검증)

| 체크 | 상태 |
|------|------|
| shell `max-w-[430px] overflow-x-hidden` | ✅ |
| carousel 190px `overflow-hidden` | ✅ |
| sticky CTA `z-30` + safe-area | ✅ |
| section nav `min-w-0` | ✅ |
| **375/390/430 DevTools 육안** | ☐ 수동 |

---

## 4. 수정 요약

| # | 이슈 | 심각도 | 조치 |
|---|------|--------|------|
| 1 | 장바구니 담기 → 로그인 후 intent 유실 | **P1** | `pending` query + join-cart server 처리 |

**수정 파일 (미커밋):**

- `components/add-to-join-cart-button.tsx`
- `app/join-cart/page.tsx`

---

## 5. 미해결 / 수동 QA

| 항목 | 상태 |
|------|------|
| OAuth Google/Kakao 실로그인 | ☐ |
| Toss 테스트 결제 1건 | ☐ |
| Vercel production smoke | ☐ |
| 375/390/430px 실기기 clipping | ☐ |
| Supabase `join_cart` 연동 환경에서 cart persist 확인 | ☐ |
| `git push origin mobile-ui` | ☐ (SSH 로컬 필요) |

---

## 6. build / lint

```
npm run lint  → PASS (tsc --noEmit)
npm run build → PASS
경고: middleware → proxy deprecation
```

---

## 7. 최종 판정

| | |
|---|---|
| **코어 buyer 플로우** | **PASS** (장바구니 login intent 버그 수정 포함) |
| **런칭 blocker (코드)** | 없음 |
| **수동 QA 잔여** | OAuth · Toss · Vercel · 실기기 viewport |

---

*Generated by CELLOH mobile-ui runtime QA · QA 우선 · 최소 수정 only*
