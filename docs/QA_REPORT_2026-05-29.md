# Wadeal v2 QA Report — 2026-05-29 (Post-DB Migration)

> 범위: Supabase SQL 030–045 적용 후 기능 QA (코드 경로·빌드·권한·DB 연결)  
> DB: 실 Supabase (`.env.local` 연결) · probe: `node scripts/probe-migrations.mjs` (env 값 미출력)  
> 사용자 확인: **038·044 포함 030–045 전체 적용 완료**

---

## 1. 통과한 기능

### DB / 마이그레이션 (probe + 코드 경로)
| ID | 대상 | probe (anon) | 코드 경로 |
|----|------|--------------|-----------|
| 030 | `business_settings` | applied | `lib/data/business-settings.ts` ✓ |
| 039 | `seller_notices` | applied | `lib/data/seller-notices.ts` ✓ |
| 040 | `seller_documents`, `seller_review_checks` | applied | seller apply / admin review ✓ |
| 041 | `category_review_rules`, `product_review_checklists` | applied | `lib/data/category-review-rules.ts`, `product-review-checklist.ts` ✓ |
| 042 | `seller_product_requests`, `seller_settlements`, … | applied | seller product-requests / settlements ✓ |
| 043 | `order_timelines` | applied | `lib/data/order-timelines.ts` ✓ |
| 045 | `refunds`, `orders.refund_status` | applied | `lib/data/refunds.ts` ✓ |
| 038 | `notifications` unified | table applied · columns anon probe partial* | `lib/notifications/unified.ts`, `lib/data/notifications.ts` ✓ |
| 044 | Storage buckets | anon probe missing* | `lib/supabase/seller-storage.ts` constants ✓ |

\* anon key probe 한계: `target_role`/`seller_id` 컬럼·`listBuckets()`는 service role 또는 Dashboard에서만 완전 확인 가능. **사용자 적용 확인 기준 applied 처리.**

### 구매자 (Buyer)
- 홈·카탈로그·카테고리·검색·상품 상세·참여·체크아웃
- 주문 내역·환불/취소 **요청** (`createRefundRequest` — buyer only)
- 알림 목록·읽음 처리 (`mark_notification_read` RPC + direct update 폴백)
- 리뷰·배송지·찜·고객센터·약관 페이지

### 판매자 (Seller)
- 입점 신청·상태 게이트 (`/seller/pending|rejected|suspended`)
- 승인 후 대시보드·상품·상품등록요청·주문·발송·공지·알림
- **`seller_id` 서버측 검증**: `getSellerAccessContext` / `requireApprovedSeller` 패턴
- 환불 **승인/반려 액션 없음** (의도된 설계)

### 관리자 (Admin)
- `requireAdmin` layout + middleware `/admin` exact path
- 환불 승인/반려: `app/actions/admin-refunds.ts` → `approveRefundRequest` / `rejectRefundRequest`
- 입점 심사·상품·주문·정산·공지·business_settings·마이그레이션 상태 페이지

### Auth
| 시나리오 | 동작 |
|----------|------|
| 미인증 → 보호 경로 | `/login?redirect=&next=` (`middleware.ts`) |
| 비관리자 → `/admin/*` | `/unauthorized?next=` |
| pending/rejected/suspended seller | `/seller/pending` · `/rejected` · `/suspended` |
| 미입점 seller | `/seller/apply` |

### 결제 / 환불
- Toss env 미설정: 위젯 비활성·503·크래시 없음 (`isTossPaymentsConfigured`, payment pages)
- 결제 success/fail 페이지: Supabase 미설정 시 안내 UI (crash 없음)
- 환불 DB 플로우: create → admin approve/reject → timeline + notification
- Toss **실환불 API**: deferred (`apiDeferred: true`)

### Storage (코드 경로)
| Bucket | 경로 빌더 | URL |
|--------|-----------|-----|
| `product-images` | `buildProductImageStoragePath` | public URL helper |
| `review-images` | `buildReviewImageStoragePath` | public URL helper |
| `seller-documents` | `buildSellerDocumentPath` | bucket 상수 (업로드 UI 연동은 seller apply 쪽) |
| `settlement-files` | `buildSettlementFilePath` | bucket 상수 |

### Branding
- app 코드: **KIBI / kibi / keyring / figure / BUNTO 잔존 없음** (grep)
- `lib/seo/site.ts`: **와딜** · `app/manifest.ts` · `/icon.svg` 일치

---

## 2. 고친 기능

| 항목 | 변경 |
|------|------|
| 알림 insert mock | `lib/notifications/create.ts` — Supabase 미설정 시 `not_configured` (가짜 success 제거) |
| 알림 읽음 | `markNotificationAsRead` — RPC missing 시 `read_at` direct update 폴백 |
| 알림 조회 | `lib/data/notifications.ts`, `unified.ts` — `target_role` 컬럼 오류 시 `user_id` 레거시 조회 |
| 알림 생성 | `lib/notifications/unified.ts` — RPC missing → direct insert (038 applied 환경에서도 안전) |
| 판매자 즉시 결제 | `paySellerBillingWithToss` mock paid 제거 → "준비 중" 메시지 |
| seller_billings | missing table/column 시 조용한 fallback + `not_configured` |
| auth bypass | unauthenticated `notifySellerSettlementReady` server action 삭제 |
| global-error | standalone HTML fallback (빌드 호환) |

---

## 3. 아직 안 되는 기능

| 기능 | 상태 | 비고 |
|------|------|------|
| Toss **실결제/실환불** live | deferred | `TOSS_SECRET_KEY` + cancel API 미연동 |
| 판매자 광고비 **즉시 Toss 결제** | deferred | 정산 차감 방식만 |
| Kakao OAuth **프로덕션** | deferred | AUTH-001 |
| 포인트·쿠폰함 UI | partial | schema/UI 준비 중 |
| order_items 라인 | deferred | 단일 `orders` row 모델 |
| 리뷰 좋아요 DB | deferred | localStorage |
| seller-documents / settlement-files **업로드 E2E** | partial | bucket·경로 상수 있음; 전체 업로드 UI 수동 QA 필요 |
| git push → Vercel prod | blocked | B003 HTTPS auth |

---

## 4. 권한 문제

**코드 감사 결과: 치명적 권한 구멍 없음**

- 환불 approve/reject: **admin only** (`admin-refunds.ts`, `/api/payments/toss/refund`)
- 판매자 환불 confirm: **액션 없음** ✓
- seller 데이터: server action에서 `sellerId` 세션 매핑 검증
- middleware + layout 이중 게이트 (admin/seller)

**수동 QA 권장**: 실계정으로 seller 계정이 `/admin/refunds` 접근 시 `/unauthorized` 확인

---

## 5. DB 연결 문제

### Probe 결과 (2026-05-29, probeMode: **anon**)

```
030 applied | 039 applied | 040 applied | 041 applied | 042 applied | 043 applied | 045 applied
038 partial (notifications table OK; target_role/seller_id column probe false on anon)
044 missing (listBuckets false on anon — service role 미설정)
```

### 해석
- **실 DB 연결 정상** — 대부분 migration applied
- 038 column probe false: anon/RLS/schema cache 한계 가능 → **사용자 Dashboard 적용 확인 우선**
- 044 bucket probe false: `SUPABASE_SERVICE_ROLE_KEY` 없음 + anon `listBuckets` 제한 → **Dashboard Storage에서 bucket 존재 확인 권장**
- `SUPABASE_SERVICE_ROLE_KEY` 미설정: server-side insert/RPC는 authenticated client fallback 사용

### 코드 fallback (crash prevention, real data 차단 최소화)
- notifications: RPC → direct insert; read 시 legacy `user_id` query
- seller_billings: missing table → `[]` / `not_configured`
- 기타 `lib/data/*`: missing table → 빈/0 (mock success 제거됨)

---

## 6. 외부연동 보류

| ID | 항목 |
|----|------|
| PAY-001 | Toss live PG + cancel API |
| AUTH-001 | Kakao OAuth prod redirect |
| B003 | Git push (HTTPS auth) |
| B004 | Vercel `--prod` |
| B005/E006 | (legacy) 원격 migration — **사용자 038/044 적용 완료 확인** |

---

## 7. TOP5 risks

1. **Toss live PG 미검증** — confirm/webhook/refund live 키 없이 운영 시 결제·환불 불일치
2. **Git push blocked** — unpushed commits; Vercel prod stale
3. **Webhook HMAC 검증 TODO** — `process-webhook.ts` must-fix
4. **Storage E2E 미수동 QA** — bucket 적용 후 서류/정산 파일 업로드 실제 테스트 필요
5. **SERVICE_ROLE_KEY 미설정** — background notification insert·일부 admin write가 session client에 의존

---

## 8. build results

| Command | Result | Notes |
|---------|--------|-------|
| `NODE_OPTIONS='--max-old-space-size=6144' npm run build` | **PASS** | Next.js 16.2.6 Turbopack |
| `NODE_OPTIONS='--max-old-space-size=6144' npx next build --webpack` | **PASS** | webpack fallback |
| TypeScript | **PASS** | both builds |
| Sandbox note | Turbopack first attempt ENOENT in sandbox — full permissions에서 성공 |

---

## 9. next steps

1. **수동 E2E** (실계정): 구매 → 결제(test) → 타임라인 → 환불 요청 → admin 승인 → 알림
2. **Storage**: seller 입점 서류 업로드·정산 파일 업로드 Dashboard에서 1회 확인
3. **`.env.local`**: `SUPABASE_SERVICE_ROLE_KEY` 추가 시 probe service_role 모드·background writes 안정화
4. **B003 해결** → `git push` → Vercel prod deploy
5. **PAY-001 / AUTH-001** — live 키·OAuth 설정 후 go-live-readiness 재점검

---

*Generated: 2026-05-29 post-migration functional QA*
